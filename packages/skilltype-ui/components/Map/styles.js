// import { theme as defaultTheme } from '../../shared-styles'

// export default (theme = defaultTheme) => ({
export default () => ({
  red: {
    '& markerPath': {
      fill: 'red',
    },
  },
})
